---
metadata:
  partial_name: Lib | Validations | Presence
---
{% comment %}
  params: @object
          @field_name
          @c
          @key[optional]
{% endcomment %}

{% liquid
  if object[field_name] == blank
    assign message = "cannot be blank"
    function c = 'modules/insites_core/lib/register_error', contract: c, field_name: field_name, message: message
  endif
  return c
%}
