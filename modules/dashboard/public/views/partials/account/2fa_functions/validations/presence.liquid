---
metadata:
  partial_name: Lib | Validations | Presence
---
{% comment %}
  params: @object
          @field_name
          @c
          @key[optional]
{% endcomment %}

{% liquid
  if object[field_name] == blank
    assign message = "cannot be blank"
    function c = 'modules/dashboard/account/2fa_functions/register_error', contract: c, field_name: field_name, message: message
  endif
  return c
%}
