---
metadata:
  partial_name: 2FA Functions | Commands | Execute
---
{% liquid
  assign selection = selection | default: 'record'

  graphql r = mutation_name, args: object

  assign object = r[selection]
  hash_assign object['valid'] = true
  return object
%}
